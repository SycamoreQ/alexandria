(library
 (name dunedb)
 (wrapped false)
 (libraries str unix ocaml-protoc-plugin))

(rule
  (targets schema.ml schema.mli)
  (deps lib/schema.proto)
  (action (run ocaml-protoc --ml_out lib/%{deps})))
